'use strict';

describe('myApp.survey module', function() {

  beforeEach(module('myApp.survey'));

  describe('survey controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var SurveyCtrl = $controller('SurveyCtrl');
      expect(SurveyCtrl).toBeDefined();
    }));

  });
});