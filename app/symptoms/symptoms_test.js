'use strict';

describe('myApp.symptoms module', function() {

  beforeEach(module('myApp.symptoms'));

  describe('symptoms controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var SymptomsCtrl = $controller('SymptomsCtrl');
      expect(SymptomsCtrl).toBeDefined();
    }));

  });
});