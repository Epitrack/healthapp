'use strict';

describe('myApp.puzzle module', function() {

  beforeEach(module('myApp.puzzle'));

  describe('puzzle controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var PuzzleCtrl = $controller('PuzzleCtrl');
      expect(PuzzleCtrl).toBeDefined();
    }));

  });
});